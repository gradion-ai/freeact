[
  {
    "name": "subagent_task",
    "parameters_json_schema": {
      "properties": {
        "prompt": {
          "description": "Task description for the subagent",
          "title": "Prompt",
          "type": "string"
        },
        "max_turns": {
          "description": "Maximum number of agentic turns before stopping. Defaults to 100.",
          "title": "Max Turns",
          "type": "integer",
          "default": 100
        }
      },
      "required": ["prompt"],
      "type": "object"
    },
    "description": "Spawn a subagent to execute a task in isolation. The subagent has the same tools and capabilities but a fresh context window and kernel. Returns the subagent's final text response. For independent tasks, spawn multiple subagents in parallel."
  }
]
